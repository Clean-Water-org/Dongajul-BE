plugins {
    id 'spring'
    id 'spring-persistence'
    id 'grpc'
}

group "${projectGroup}.mentoring"
version "${applicationVersion}"

bootJar.enabled = true
jar.enabled = false

dependencies {
    implementation project(':grpc-proto')
    implementation project(':common')
}