rootProject.name = 'dongajul-be'

file("${rootDir}").eachDir {
    if (it.directory
            && new File(it.absolutePath, "build.gradle").exists()
            && it.name != 'buildSrc'
    ) {
        include(it.name)
    }
}

include('router:discovery')
include('router:gateway')