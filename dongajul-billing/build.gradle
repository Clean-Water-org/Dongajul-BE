plugins {
    id 'spring'
    id 'spring-persistence'
}

group "${projectGroup}.billing"
version "${applicationVersion}"

bootJar.enabled = true
jar.enabled = false

dependencies {
    implementation project(':common')
}